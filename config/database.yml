# config/database.yml — FINAL VERSION THAT CANNOT BE BROKEN
default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  host: localhost
  port: 5432
  username: super_admin
  password: SomeSecretPassword
  database: weather_forecast_app_development

test:
  <<: *default
  database: weather_forecast_app_test

production:
  <<: *default
  # FORCE these values — no matter what DATABASE_URL says
  host: <%= ENV.fetch("DATABASE_HOST") { "weatherforecast-db.cxsikmmum8rh.us-east-2.rds.amazonaws.com" } %>
  port: 5432
  username: <%= ENV.fetch("DATABASE_USERNAME") { "postgres" } %>
  password: <%= ENV["DATABASE_PASSWORD"] %>
  database: <%= ENV.fetch("DATABASE_NAME") { "weatherforecast_production" } %>

  # Optional: if you ever want to use DATABASE_URL instead, set it properly
  # url: <%= ENV["DATABASE_URL"] if ENV["DATABASE_URL"].present? %>